import 'package:flutter/material.dart';

class foodtrackerBody extends StatelessWidget {
  const foodtrackerBody({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

      child: Center(child: Text("deneme")) ,
    );
  }
}